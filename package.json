{
  "name": "axiom-worksheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Mathcad-inspired engineering worksheet with an independent numerical kernel and WebGPU plot rendering.",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080",
    "validate": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
